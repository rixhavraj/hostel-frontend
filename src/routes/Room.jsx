export default function Rooms() {
  return (
    <div className="mx-auto max-w-6xl px-4 py-10 space-y-8">
      <header className="space-y-2">
        <h1 className="text-3xl font-semibold tracking-tight">All Room Types</h1>
        <p className="text-sm text-slate-600">
          Transparent pricing, clear inclusions and limited seats in each
          category.
        </p>
      </header>

      {/* reuse same card layout as Home; in real project you’d map over JSON */}
      {/* ... */}
    </div>
  );
}
